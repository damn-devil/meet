# … — планы для двоих 💜

Веб-приложение (PWA) для пары: совместные планы, оценки и общий фон для обоих.

## Что умеет

- **Пара из двух человек** — отдельный раздел «Пара»: один находит второго по
  имени/никнейму и отправляет запрос, второй получает уведомление и соглашается
  или отказывается; в любой момент пару можно разорвать.
- **Планы** — название («что делаем»), комментарий и, по желанию, дата и время.
  Время **необязательно** — план можно создать без времени.
- **Приход** — отмечается вручную кнопкой «Я на месте»: когда **оба** человека
  отметились, план автоматически засчитывается выполненным.
- **Пропуск** — просроченные планы автоматически переводятся в «пропущено»
  через 2 часа после времени.
- **Перенос и удаление — только с согласия обоих**. Один отправляет запрос,
  второй подтверждает.
- **Оценка встречи** — после того как встреча состоялась, каждый ставит 1–5 звёзд.
- **Плавающее меню навигации** — три раздела: «Планы», «Пара», «Профиль».
- **Общий фон из фото** — в «Основных настройках» загрузите любую фотографию,
  она станет фоном приложения у обоих партнёров (хранится в паре и синхронизируется
  через Realtime).
- **Редактирование профиля** — имя, аватар (эмодзи **или фото из галереи** —
  грузится в Supabase Storage), «о себе», а также контакты
  **Telegram** и **iMessage**: на карточке партнёра появляются кнопки,
  которые одной кнопкой открывают чат с ним.
- **Основные настройки** — кнопка в профиле: темы **авто/светлая/тёмная**
  (локальная, хранится только на устройстве), акцентный цвет (пресеты iOS +
  **свой цвет** через цветовую палитру, сохраняется в облаке), фон из фото,
  iOS-переключатель уведомлений — удалён вместе с системными уведомлениями.
- **Брутал-режим** — тумблер в «Основных настройках»: переключает весь интерфейс
  на нео-брутализм — резкие рамки, тени-сдвиги, коробчатые кнопки, терминальные
  поля ввода, пилюльное меню и собственные переключатели. Локальная настройка
  устройства.
- **Уже во время открытого приложения** — встроенные тосты мгновенно
  сообщают о новых планах, оценках, приходах, запросах на перенос/удаление
  и приглашениях в пару (через Supabase Realtime). Фоновых push-уведомлений нет.
- **Удаление аккаунта** — в профиле; удаляет пару, планы и все данные без
  возможности восстановить (у партнёра профиль остаётся).
- **PWA** — устанавливается на iPhone («На главный экран»), работает офлайн (каркас).

## Превью дизайна

Дизайн выполнен по **Apple HIG**: стеклянные поверхности (backdrop blur 40px),
системный синий `#007AFF`, плавающая круглая навигация, модальные окна-шторки снизу,
переключатели-тогглы, скругления 22px/44px.

Посмотреть дизайн офлайн (без Supabase) — просто откройте в браузере
**`design-preview.html`** (двойной клик по файлу): обычный сайт (без рамки
телефона) на цветном mesh-фоне со стеклянными карточками, экраны входа, планов,
деталей, пары, профиля, нижняя панель «Новый план», тёмная/светлая тема,
свой цвет акцента и фон из фото (сохраняются в localStorage).

## Архитектура

- **Frontend** — React + Vite, статический сайт на **GitHub Pages**.
- **Backend** — **Supabase** (PostgreSQL): вся бизнес-логика живёт в SQL-функциях
  (RPC), права доступа — через Row Level Security, живая синхронизация между
  партнёрами — через Supabase Realtime. Вход — по email/паролю (Supabase Auth).

```
src/
  lib/supabase.js    клиент Supabase (URL + anon-ключ из .env)
  api.js             обёртки над RPC-функциями + Realtime-подписка
  screens/           экраны (планы, пара, профиль, детали)
  components/        Avatar, модалки
  lib/               темы (вкл. брутал-режим), контакты, форматирование
  test/              тесты компонентов
supabase/schema.sql  вся схема БД: таблицы, RLS, RPC, Realtime, storage
public/              PWA (manifest, service worker, иконки)
```

## Быстрый старт (локально)

```bash
npm install
# создайте .env по образцу .env.example с данными вашего Supabase-проекта
npm run dev          # Vite на http://localhost:5173
```

## Настройка продакшена (Supabase + GitHub Pages)

### 1. Supabase

1. Создайте проект на https://supabase.com (Free-план подходит).
2. Откройте **SQL Editor**, вставьте целиком содержимое `supabase/schema.sql`
   и выполните. Это создаст таблицы, RLS-политики, RPC-функции и включит Realtime.
3. **Authentication → Providers → Email**: включите провайдера. Чтобы вход был
   мгновенным (без письма), выключите **Confirm email**. Если оставите подтверждение —
   при регистрации придёт письмо со ссылкой, после подтверждения нужно войти.
4. **Realtime**: добавьте таблицы `couples`, `tasks`, `checkins`
   в публикацию Realtime (Dashboard → Database → Replication) — либо
   раскомментируйте строки `alter publication supabase_realtime ...` в конце
   `schema.sql` и выполните скрипт ещё раз.
5. Заберите в **Settings → API**: `Project URL` (в переменную `VITE_SUPABASE_URL`)
   и `anon public` key (в `VITE_SUPABASE_ANON_KEY`).

### 2. GitHub Pages

1. Создайте репозиторий и запушьте код (ветка `main`).
2. **Settings → Secrets and variables → Actions**: добавьте секреты
   `SUPABASE_URL` и `SUPABASE_ANON_KEY`.
3. **Settings → Pages**: Source = **GitHub Actions**.
4. Пуш в `main` запустит workflow `.github/workflows/deploy.yml`: соберёт проект
   и опубликует на `https://ВАШ_ЛОГИН.github.io/РЕПОЗИТОРИЙ/`.

Пуш дополнительно в `supabase/schema.sql` — просто перевыполните его в SQL Editor.

## Установка PWA на iPhone

1. Откройте адрес GitHub Pages на iPhone.
2. «Поделиться» → «На главный экран» — приложение установится как PWA.

## Тесты

```bash
npm test           # все фронтенд-тесты (vitest + jsdom)
npm run test:web   # то же самое
npm run lint       # oxlint
npm run build      # сборка в dist/
```

## Синхронизация

Устройства пары синхронизируются в реальном времени через Supabase Realtime:
события `tasks`, `couples` и `checkins` автоматически доставляются обоим
партнёрам.
